# Audit & Compliance: Form Builder

## Logged Events
- `form.create`, `form.publish`, `form.deprecate`.

## Fields
- `diff` (JSON Diff of changes).
- `approved_by` (If approval workflow used).

## Retention
- Form Definitions are **Never Deleted**. They are essential for interpreting historical data.
